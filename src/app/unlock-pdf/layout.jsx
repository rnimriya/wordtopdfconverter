export const metadata = {
  title: 'Unlock PDF | 100% Secure & Private Local Processing',
  description: 'Use our Unlock PDF tool instantly in your browser. No server uploads, no file size limits, and 100% data privacy with our local WebAssembly engine.',
};

export default function Layout({ children }) {
  return children;
}
