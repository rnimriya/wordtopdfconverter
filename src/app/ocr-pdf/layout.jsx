export const metadata = {
  title: 'OCR PDF (Text Recognition) | 100% Secure & Private Local Processing',
  description: 'Use our OCR PDF (Text Recognition) tool instantly in your browser. No server uploads, no file size limits, and 100% data privacy with our local WebAssembly engine.',
};

export default function Layout({ children }) {
  return children;
}
