export const metadata = {
  title: 'PDF to PDF/A | 100% Secure & Private Local Processing',
  description: 'Use our PDF to PDF/A tool instantly in your browser. No server uploads, no file size limits, and 100% data privacy with our local WebAssembly engine.',
};

export default function Layout({ children }) {
  return children;
}
