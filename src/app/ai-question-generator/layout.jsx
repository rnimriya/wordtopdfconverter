export const metadata = {
  title: 'AI Question Generator | 100% Secure & Private Local Processing',
  description: 'Use our AI Question Generator tool instantly in your browser. No server uploads, no file size limits, and 100% data privacy with our local WebAssembly engine.',
};

export default function Layout({ children }) {
  return children;
}
